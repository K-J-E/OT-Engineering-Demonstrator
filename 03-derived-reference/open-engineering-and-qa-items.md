---
Status: Active tracking register
Authority: Derived reference only
Owner: Project engineering review process
Updated: 2026-08-12
---

# Open Engineering and QA Items

## Purpose and authority

This register is a tracking and assurance aid only. It does not override the governing documents, detailed engineering source documents, requirements, architecture, workflow, demonstrator design or accepted design-change records. It is not a replacement for formal design-change control.

Statuses below have been reconciled against accepted DC-004/DC-005 design and application, accepted DC-006 design and authoritative-document application at exact reviewed technical tip `c19451134c36d13d54f2185a3eaa0f20fcce95f0`, and the independently accepted I1–I8 implementation baselines. Final independent re-review closed QA-050 at reviewed application tip `4ba232a14b4a5a6a8349fe5bc5eab1631ba5cb47`; QA-051 and QA-052 remain independently closed at `c7879bd59a745e1f361cdf756c9271b93c79b661`. The bounded QA-053–QA-055 conformance corrections are complete on the same dedicated branch and await final independent application re-review. The DC-006 machine/catalogue application is not yet an accepted baseline and I9 remains stopped. A closed item is not reopened unless an authoritative repository artefact contains an actual contradiction.

## Register

| ID | Item | Reconciled status | When to deal with it | Required treatment / repository finding |
|---|---|---|---|---|
| **QA-001** | Requirements-count arithmetic was historically wrong: original 115 → DC-001 117 → DC-002 124, rather than 114 → 116 → 123. | **Closed** | Nothing further unless an outdated copy resurfaces. | The current Requirements Specification and downstream allocation records contain 124 unique formal requirements. No current contradiction was found. |
| **QA-002** | DC-001 Word-document revision headers historically remained v0.1 even though the logical revision should have become v0.2. | **Closed / documented** | Nothing further unless final document-control history is polished. | DC-002 documents the inherited discrepancy and the current controlled sequence is coherent. Git history must not be rewritten merely to restate old headers. |
| **QA-003** | Distinguish configured/nominal feeder load from derived currently supplied load, particularly because defective v1.0 makes FDR-B actually supply 5.70 MW although its configured normal load is 4.20 MW. | **Accepted through I6 baseline** | Regression watch. | The accepted I2 baseline uses separate `configured_normal_load_kw` and `currently_supplied_load_kw` fields. The independently accepted I6 presentation preserves separate configured and derived contracts, labels and feeder-table columns; the browser workflow proves FDR-B remains configured at 4.20 MW while its N5 derived supplied load is 5.70 MW. No frontend load calculation exists. |
| **QA-004** | Network Model traceability lists selected `REQ-EXP` requirements rather than mechanically listing the entire Exploration group. | **Watch only — not an error** | Step 9 RTM and final validation audit. | Step 9 maps every `REQ-EXP-*` requirement to verification. Specific traceability is preferred over attaching every requirement to every artefact. |
| **QA-005** | The Network Model may eventually state explicitly that Sections 1–17 form the approved network baseline. | **Document-control polish** | When working Draft engineering documents move to Baseline/Approved status. | Update status language during final baselining. This is not currently a design change. |
| **QA-006** | Architecture terms such as “ADMS Topology” and “ADMS Restoration Assessment” denote logical functions, not claims that they are separate deployed utility products. | **Resolved / watch only** | Implementation and interview preparation. | Step 8 preserves these as logical modules in one local demonstrator. Continue using conceptual wording. |
| **QA-007** | Operational events must remain separate from validation PASS/FAIL, defect/correction and engineering-judgement records. | **Accepted through I8 baseline** | Later regression watch. | I5 stores executions/checkpoints/evidence in validation-owned contracts and tables. I7 adds separate immutable defect, correction and repeat-link records. Accepted I8 adds a separate immutable package register and preserves the exact 15 operational event types without adding export, defect, correction or verdict events. |
| **QA-008** | Evidence export format was initially deferred. | **Accepted as implemented — I8 baseline** | Later package/final-campaign regression watch. | Accepted I8 implements the approved new, non-overwriting self-contained ZIP with `report.html`, canonical JSON records, preserved-evidence figure, README and independently verifiable SHA-256 manifest. The backend accepts only a preserved execution identity, retains FORMAL/EXPLORATORY classification and provenance, and records each verified package immutably. Final independent review accepted the treatment. |
| **QA-009** | Application build identity is required to prove DEF-001 v1.0 FAIL → v1.1 PASS without a code change. | **Accepted through I8 baseline** | Full-campaign regression watch. | The I7 chain retains backend-controlled I1/I3/I5 provenance. Accepted I8 exports the stored source build separately from the current package-generation build, preserving historical QA-037 reviewability; no caller supplies either build identity. |
| **DQ-001** | Exploration fault-isolation action derivation is not completely explicit for arbitrary selected sections. | **Closed by accepted/applied DC-003** | Implementation and validation watch only. | DC-003 is applied across the six affected authoritative artefacts and cross-document verified. It defines generic incident boundaries, A/B/C evidence/action conditions, one-action recalculation and the all-open plus zero-source-path proof without hard-coded outcomes. |
| **DQ-002** | The accepted validation model binds one execution to one run but did not define the catalogue-level determination for `VT-EXP-ALL-001` and `VT-EXP-ROLE-001`, whose controlled evidence spans multiple exploratory runs. | **Resolved and independently accepted through DC-004 application baseline** | Regression watch. | The accepted application preserves one-run execution provenance, implements the exact 9/4 case sets and immutable composite determination, resolves accepted catalogue revisions by stored identity, keeps unfinished old-catalogue executions read-only, separates engineering comparison from dynamic provenance and retains constituent-owned scenario time. No electrical behaviour, RTM allocation or event type changed. |
| **VP-001** | Complete formal validation test catalogue beyond `VT-TOP-DEF-001` was deliberately undefined. | **Closed by accepted Step 9 baseline** | Execute after implementation is separately authorised and reaches the relevant test boundary. | Validation Plan v1.0 accepts the 24-test catalogue and controlled procedures. |
| **VP-002** | Controlled per-step scenario-time increments were deliberately undefined. | **Closed by accepted Step 9 baseline** | Implementation and execution watch. | Validation Plan v1.0 accepts the deterministic UTC scenario clock and exact 60.000 s / 60.001 s boundary cases. |
| **VP-003** | Validation must cover all 124 requirements, including negative cases where correct operational failure/blocking yields validation PASS. | **Closed by accepted Step 9 baseline** | Execute and maintain coverage during implementation. | The accepted RTM contains exactly 124 unique requirement rows; targeted mappings for REQ-RST-005, REQ-TEL-003 and REQ-TEL-007 remain corrected. |
| **VP-004** | Exploration Mode is not formal scenario evidence, but the `REQ-EXP-*` requirements still require verification. | **Closed by accepted Step 9 baseline** | Execute after implementation is separately authorised and reaches Exploration verification. | The accepted plan verifies all-section/common-engine/evidence-class behaviour and the SEC-A4 trustworthy-OPEN versus untrustworthy-last-OPEN subcase. |
| **IMP-001** | Exact package/dependency patch versions are intentionally not selected. | **Closed by accepted I1 baseline** | Reopen only through controlled dependency change and regression impact review. | I1 pins Python 3.13.15, Node.js 24.19.0, npm 11.17.0, setuptools 84.0.0 and every backend/frontend direct and transitive dependency in `pyproject.toml`, `requirements.lock`, `package.json` and `package-lock.json`. Their lock hashes and toolchain versions feed the application build identity. Clean Python and npm installations were independently reviewed and accepted. |
| **IMP-002** | Apply accepted DC-004 to the controlled machine catalogue, validation contracts, historical resolution, composite persistence, review and export boundaries without resuming I9. | **Independently accepted / incorporated into reviewed main** | Regression watch. | Exact v1.0 catalogue/manifest bytes are preserved; corrected active v1.1 retains 24 tests, 124 requirements and 286 RTM pairs; exact 9/4 cases, case-bound executions, historical read-only treatment, immutable composites and historical/composite export passed final review at tip `eced7c06…5e258`. No dependency, network package, electrical algorithm or event-type change occurred. |
| **IMP-003** | Apply accepted DC-006 to the machine Validation Catalogue and validation-determination application without resuming I9. | **QA-050–QA-052 independently closed; QA-053–QA-055 corrected pending final re-review** | Final independent re-review of the dedicated DC-006 application branch. | Active branch catalogue v1.2 remains exactly 35 methods / 214 criteria / 24 tests / 124 requirements / 286 RTM pairs / 15 events. Historical v1.0/v1.1 remain byte-identical. Migrations 011–013 and bounded backend/API/frontend/export changes implement registered backend source producers/origin bindings, executable normalisation, one real execution per performed non-composite procedure, contexts, findings, review controls, deterministic aggregate and immutability. The final corrections close catalogue-wide producer/selector paths, exact repeat/package role membership and actual eight-surface/45-record NFR evidence. No authoritative DOCX, network package, dependency version or engineering algorithm changed. This status does not claim acceptance or I9 campaign execution. |
| **QA-010** | README and current-baseline manifest needed independent inspection after Step 8. | **Closed** | Nothing further unless those files change inconsistently. | Both files were inspected against the repository. The Step 8 Demonstrator Design hash, size, version and structural counts in the manifest match the actual file; README describes the same working baseline. |
| **QA-011** | DC-003 did not explicitly distinguish trustworthy OPEN, trustworthy CLOSED and an untrustworthy last-reported boundary state. | **Closed by accepted/applied DC-003** | Implementation and validation watch. | Conditions A/B/C are applied consistently across the six authoritative artefacts. An untrustworthy boundary is UNPROVEN, blocks isolation and does not cause a redundant OPEN command; valid evidence/commands trigger full recalculation. |
| **QA-012** | `REQ-RST-005 — Alternate Source Availability` used the isolation negative rather than the dedicated alternate-source negative. | **Closed in revised Step 9 draft** | Reconfirm during independent RTM review. | The authoritative requirement wording remains unchanged. RTM mapping is now `VT-FML-N0-N5-001` plus `VT-RST-SOURCE-001`. |
| **QA-013** | Telemetry-quality RTM coverage was incomplete for `REQ-TEL-003` and `REQ-TEL-007`. | **Closed in revised Step 9 draft** | Reconfirm during independent RTM review. | `REQ-TEL-003` maps to fresh, UNCERTAIN and BAD cases. `REQ-TEL-007` maps to fresh, stale, future/invalid timestamp, UNCERTAIN and BAD cases. |
| **QA-014** | Capacity equality, 1-kW exceedance and proposed-loop tests could be misread as runtime mutation of the canonical network. | **Closed in Step 9; I4 treatment accepted** | Regression watch. | I4 implements the 6,000/6,001 kW and proposed-loop cases only as lower-level domain fixtures. Canonical packages remain byte-identical and no runtime editor or arbitrary load/topology mutation was introduced. |
| **QA-015** | DEF-001 wording implied that the running demonstrator creates v1.1 and later wording overstated that implementation packages already existed. | **Closed in Step 9 and accepted I1 baseline** | Runtime and evidence watch in later increments. | Accepted I1 contains separate schema-valid, hash-verified v1.0 and v1.1 implementation packages. The loader reads either package independently and never creates, rewrites or derives v1.1 from v1.0. |
| **QA-016** | `VT-RST-ISOLATION-001` implied successful isolation immediately enabled alternate restoration assessment. | **Closed in revised Step 9 draft** | Reconfirm during workflow review. | Proven formal SEC-A2 isolation enables the approved next action, BRK-A reclose/N2→N3. Alternate restoration assessment remains unavailable until N3 preconditions are satisfied. |
| **QA-017** | `VT-RST-SOURCE-001` used a vague unavailable-source condition. | **Closed in Step 9; I4 treatment accepted** | I5 execution/regression watch. | I4 proves the exact negative: structural candidate remains identified with ZS-01 AVAILABLE and trustworthy/fresh BRK-B OPEN, and the operational assessment is REJECTED rather than NO_CANDIDATE or BLOCKED. No formal validation verdict is created before I5. |
| **QA-018** | The initially evaluated newest frontend compiler and the Python editable-install path were not compatible with the pinned I1 toolchain. | **Closed by accepted I1 baseline** | Dependency-change and developer-setup watch. | TypeScript 7.0.2 produced an internal compiler failure in the scaffold build, so IMP-001 pins the verified compatible TypeScript 6.0.3. Python 3.13.15 skips the hidden `.pth` file emitted by setuptools 84.0.0 for editable installation, so the controlled setup uses a normal wheel installation; clean installs, imports, packaged migration access, tests and builds pass. No engineering behaviour changed. |
| **QA-019** | Hash-protected repository text could acquire platform-dependent CRLF working-tree bytes because the existing Git policy normalized text without forcing checkout LF. | **Independently verified closed — accepted I1 baseline** | Repository and controlled-input integrity watch. | `.gitattributes` now forces `text=auto eol=lf`, explicitly marks controlled text/source/SQL/lock formats as LF and authoritative/common binary formats as binary. `git add --renormalize .` changed only `.gitattributes`; all controlled files were already index/working-tree LF, configuration/schema hashes remained unchanged and no manifest recalculation was required. Full package/hash/tamper/single-difference verification passed and was independently accepted. |
| **QA-020** | The generic manifest contract did not explicitly reject a validly formatted `configuration_id` that referred to a different version than the manifest's `version`. | **Independently verified closed — accepted I1 baseline** | Configuration-contract regression watch. | `ConfigurationManifest` derives the expected identity generically as `network-configuration-v{version}` and rejects mismatch without version-specific cases. A JSON-boundary negative test proves a v1.0 identity paired with version 1.1 is rejected; both canonical packages still load and their engineering bytes/hashes are unchanged. The correction was independently accepted. |
| **QA-021** | A naive undirected graph traversal through the common `ZS-01` source entity could manufacture a feeder-to-feeder path even while `TS-01` is OPEN, contradicting the approved normal topology and source attribution. | **Independently verified closed — accepted I2 baseline** | Topology regression watch. | I2 retains the common source as availability/configuration evidence but treats each feeder breaker as a distinct source-injection boundary for distribution traversal. Tests prove v1.1 normal one-source attribution, v1.0 defect propagation and a genuine multiple-source invalid topology when the tie closes. No canonical configuration was changed. |
| **QA-022** | Radiality processing initially classified any cyclic active-device component as `UNINTENDED_LOOP`, including a cycle with zero active source injections. | **Independently verified closed — accepted I2 baseline** | Radiality regression watch. | The generic predicate now reports a cycle only when its component has an active feeder-source injection; multiple active injections remain invalid. Focused tests prove an energised cycle fails, the same de-energised cycle does not, and the both-source/tie-closed negative remains detected. |
| **QA-023** | OMS initially accepted topology/customer mappings without matching configuration identity and calculated restored-customer delta by subtracting aggregate affected totals. | **Independently verified closed — accepted I2 baseline** | Outage provenance/transition regression watch. | OMS now consumes `LoadedConfiguration`, rejects mismatched topology or previous-outage configuration IDs, and sums customers only for previously affected section/zone identities no longer affected. A composition-change test proves 180 restored while affected total increases from 400 to 480. |
| **QA-024** | Whole-second controlled timestamps initially serialized without the required explicit `.000` millisecond component even though the in-memory value was millisecond-aligned. | **Independently verified closed — accepted I3 baseline** | Timestamp-contract regression watch. | `UtcMillisecondInstant` now validates UTC/millisecond alignment and uses a controlled JSON serializer that always emits exactly three fractional digits plus `Z`. Focused serialization and 0/59,999/60,000/60,001/−1 ms tests pass and were independently accepted. |
| **QA-025** | The first I3 command-envelope draft relied on the API path for run identity, so run identity did not participate directly in the immutable request hash. | **Independently verified closed — accepted I3 baseline** | Command-provenance regression watch. | `ScenarioCommandRequest` now carries `scenario_run_id`; the coordinator rejects path/envelope mismatch, the request hash includes the run ID, and cross-run or changed-content reuse of a command ID is rejected without state/event change. The correction was independently accepted. |
| **QA-026** | The first I3 allowed-action draft iterated the controlled formal isolation procedure sequence rather than first taking the actual incident-boundary set returned by I2. | **Independently verified closed — accepted I3 baseline** | DC-003 action-authority regression watch. | The final action assembler consumes only `IsolationProof.incident_boundary_device_ids` and their I2 evaluations. The formal SW-A12→SW-A23 definition supplies deterministic procedure ordering only for boundaries actually derived from configuration; it is not an incidence lookup or stored topology result. The correction was independently accepted. |
| **QA-027** | The first I3 initialisation contract accepted a caller-supplied `application_build_id`, allowing run provenance to be self-declared instead of controlled by the backend. | **Independently verified closed — accepted I3 baseline** | Build-provenance regression and later I5/I7 watch. | `InitialiseRunRequest` no longer exposes the field. `ScenarioCoordinator` requires the trusted I1 `ApplicationBuildManifest` at construction, records that identity on the run and preserves it through reset. Positive tests prove the controlled ID is recorded; a negative public-contract test proves a caller cannot supply a false ID. The correction was independently accepted. |
| **QA-028** | A time-only alarm acknowledgement could return current telemetry validity and allowed actions beside the older persisted, time-sensitive isolation proof for the unchanged state revision. | **Independently verified closed — accepted I3 baseline** | Current-versus-historical projection regression watch. | The current snapshot recomputes topology/isolation from current configuration, telemetry and controlled scenario time while leaving the persisted revision snapshot unchanged. A 61-second regression proves unchanged `state_revision`, no topology/outage event, STALE telemetry, UNPROVEN boundaries, blocked isolation actions and preserved historical proof. The correction was independently accepted. |
| **QA-029** | I4 capacity assessment converted `currently_supplied_load_kw = None` to zero when I2 deliberately reported incomplete feeder-load attribution. | **Independently verified closed — accepted I4 baseline** | Restoration evidence/capacity regression watch. | Incomplete attribution now produces no `RestorationCalculation`, no fabricated load/percentage, capacity `INSUFFICIENT` with `CURRENT_FEEDER_LOAD_UNATTRIBUTABLE` and overall `BLOCKED`. A focused fixture proves zero is not substituted and insufficient evidence retains BLOCKED precedence even with another conclusive failed criterion. |
| **QA-030** | A stale/mismatched restoration execution created an invalidation event, but the rejected immutable command result reported `new_event_ids = ()`. | **Independently verified closed — accepted I4 baseline** | Command/event/idempotency regression watch. | The invalidation event ID is now present in the returned and stored `CommandResult.new_event_ids`. Identical replay returns the same result and creates no second event or invalidation; ordinary event-free rejections remain unchanged. |
| **QA-031** | Finalised validation executions rejected evidence update/delete but did not reject inserting a new evidence snapshot against the finalised parent. | **Independently verified closed — accepted I5 baseline** | Evidence-immutability regression watch. | Migration 004 now has a database-level `BEFORE INSERT` guard that rejects evidence when the parent execution is `FINALISED`. Regression proves ACTIVE capture succeeds, late otherwise-valid insertion fails, no row survives and final `evidence_snapshot_ids` exactly matches returned evidence rows. Independent re-review accepted the correction at I5 branch tip `ae4d39102755c733def73746331c4e602845e2d0`. |
| **QA-032** | The initial I5 RTM test proved 124-ID union/group coverage but not the exact accepted requirement↔test relationship. | **Independently verified closed — accepted I5 baseline** | Traceability regression watch. | An independent test-only oracle derived directly from Validation Plan Section 15 verifies all 286 `(test_id, requirement_id)` relationships with canonical fingerprint `53ecf30…604c8`. A wrong-test mutation retaining all 124 requirements and 286 relationships fails. The current catalogue matches exactly; no mapping changed. Independent re-review accepted the correction at I5 branch tip `ae4d39102755c733def73746331c4e602845e2d0`. |
| **QA-033** | The strict I3/I5 domain request models correctly require typed UUID, enum and UTC-time objects, but the first I6 HTTP assembly passed raw JSON strings directly into those models, so browser initialisation was rejected at the transport boundary. | **Independently verified closed — accepted I6 baseline** | I6 API/browser regression watch. | Independent review accepted the explicit non-authoritative JSON transport payloads that parse wire values and convert them into unchanged strict domain contracts. The rerun browser flow still traverses initialisation, formal commands and checkpoint requests to N5. No domain model, engineering behaviour or provenance rule was weakened. Independent re-review accepted the revised I6 branch at tip `36c2aa8568eacddc5505677609c812706ec9c345`. |
| **QA-034** | I6 labelled validation progress FORMAL while the initial aggregator counted all FORMAL and EXPLORATORY catalogue definitions/executions together. | **Independently verified closed — accepted I6 baseline** | Formal-progress regression watch. | The backend now filters definitions and executions by FORMAL evidence class before calculating definitions-without-execution, execution, active/finalised, PASS, FAIL and BLOCKED-TEST totals. A regression injects exploratory ACTIVE, finalised PASS, FAIL and BLOCKED-TEST records and proves every FORMAL total is unchanged. The UI separately states 21 FORMAL of 24 total controlled definitions; exploratory records remain retained but have no I8 presentation. Independent re-review accepted the correction at I6 branch tip `36c2aa8568eacddc5505677609c812706ec9c345`. |
| **QA-035** | The persistent context ribbon visibly showed only the short run UUID; the complete identity required hover or assistive-text access. | **Independently verified closed — accepted I6 baseline** | Run-provenance presentation regression watch. | The ribbon now visibly shows both the compact short ID and complete scenario-run UUID. Component and Chromium coverage prove the full identity is visible without hover and remains unchanged from N0 through N5 across the persistent workspace context. Independent re-review accepted the correction at I6 branch tip `36c2aa8568eacddc5505677609c812706ec9c345`. |
| **QA-036** | The accepted I3 RESET operation deliberately creates a new run using the same controlled configuration, while I7 must preserve the v1.0 run and start the corrected repeat with the already-instantiated v1.1 package. | **Accepted — I7 baseline** | Reset/history regression watch. | A bounded coordinator operation closes and preserves the current run, uses only existing event types and initialises a separately identified run from the selected existing package under the same backend-controlled build. It never edits, derives or generates configuration; integration tests prove v1.0 history remains queryable and v1.1 is loaded through the accepted configuration authority. Final independent review accepts the treatment. |
| **QA-037** | The first I7 historical workspace read required the preserved failed execution's build ID to equal the currently executing application build, making an otherwise complete immutable chain unreadable after a later build was installed. | **Independently verified closed — accepted I7 baseline** | Historical-review and build-provenance regression watch. | Historical reads validate preserved execution identity without claiming it belongs to the current build; completed-chain same-build proof uses the stored failure/repeat/regression execution build IDs. Every mutating defect/correction/repeat/regression boundary still requires current-build equality, and the historical workspace exposes only `HISTORICAL_BUILD_READ_ONLY` actions. Tests reopen a complete build-A chain under build B and reject every unfinished build-A continuation path under build B. Final independent review verified the correction. |
| **QA-038** | The initial I7 replacement-run path closed the prior run without applying the accepted reset treatment to an existing current restoration assessment. | **Independently verified closed — accepted I7 baseline** | Replacement/reset assessment-history regression watch. | Reset and replacement share one history-preserving close operation. An N4 regression proves the current PERMITTED assessment remains preserved, an immutable invalidation record is linked to the existing `RESTORATION_ASSESSMENT_INVALIDATED` event, `SCENARIO_RESET` follows, the prior run is CLOSED and the new controlled run starts at N0. No 16th event type was added. Final independent review verified the correction. |
| **QA-039** | The I7 frontend's first same-build success sentence named the corrected regression as soon as failure/direct-repeat provenance was proven, even when no regression record yet existed. | **Independently verified closed — accepted I7 baseline** | Presentation regression watch. | Backend provenance calculation is unchanged. React branches wording only on backend `same_build_proven` plus backend-provided regression-record presence: before regression it names failure and direct repeat only; after regression it names all three records. Two component tests and the Chromium I7 workflow prove both states. Final independent review verified the correction. |
| **QA-040** | Exploration incident-boundary OPEN eligibility was order-dependent: an earlier lexical `UNPROVEN` boundary suppressed a later independently `PROVEN_CLOSED` incident boundary. | **Independently verified closed — accepted I8 baseline** | DC-003 and Exploration action-authority regression watch. | EXPLORATION now evaluates each actual configuration-derived incident boundary independently for action eligibility. `PROVEN_OPEN` is satisfied without redundant action, `PROVEN_CLOSED` is individually OPEN-eligible and `UNPROVEN` is individually blocked while overall isolation remains unproven. Command acceptance uses the same gate as the projection. Both asymmetric SEC-A2 permutations prove lexical order cannot suppress the eligible boundary, and post-OPEN recalculation preserves the other UNPROVEN boundary. FORMAL `_next_isolation_target()` ordering and N0–N5 behaviour are unchanged. Final independent review verified the correction closed. |
| **QA-041** | The DC-004 application branch's `EXP-ALL-A4-STALE-OPEN` integration case used 61,000 ms and categorical STALE evidence rather than machine-proving the accepted exact 60,001 ms condition. | **Independently verified closed — accepted on PR #10** | Regression watch. | Boundary evidence preserves `age_ms`; actual comparison regression proves 60,001 ms STALE/PASS, 61,000 ms categorical STALE but exact-value FAIL, and 60,000 ms FRESH. Independent re-review accepted this correction. |
| **QA-042** | The former baseline lacked the controlled constituent suspension lifecycle required for composite BLOCKED-TEST determination. | **Independently verified closed — accepted DC-005 application baseline** | Regression watch. | Final review accepted the complete controlled suspension lifecycle, persistence, composite union, classifier authority and export treatment at exact PR #10 tip `eced7c06…5e258`. |
| **QA-043** | VSC-003/VSC-005 and runtime VSC-004 facts and backend actor identities could be supplied by the caller rather than established by a backend verifier. | **Independently verified closed — accepted DC-005 application baseline** | Regression watch. | Independent re-review confirmed the caller-assertion correction is sound: backend authorities derive the facts, healthy inputs are rejected and callers cannot provide failure codes/evidence/backend actors. |
| **QA-044** | VSC-001/VSC-002 evidence accepted free-form DQ, conflict, source and hash assertions without resolving them to actual controlled records. | **Independently verified closed through QA-046** | Regression watch. | Controlled DQ/conflict/source resolution and QA-046 exact statement/step fingerprints now bind reviewer judgement to actual controlled records without machine interpretation. Independent re-review closed the combined treatment. |
| **QA-045** | VSC-003 representation did not support unresolved identity for all six accepted required-input roles or the exact DC-004 composite exception. | **Independently verified closed** | Regression watch. | All six roles, the exact unavailable-role exception and the bounded serialization correction are independently accepted. `resolved_source_identities` contains only genuinely resolved target evidence; it neither serializes an unavailable value as `"None"` nor backfills the failed role. |
| **QA-046** | Judgement evidence lacked exact canonical statement and controlled-step fingerprints. | **Independently verified closed** | Regression watch. | Controlled source assertions bind whole-document hash, exact location, canonical assertion text hash and record fingerprint. Pre-entry time review likewise binds step reference, canonical step-text hash and record fingerprint. Independent re-review accepted the treatment and closed QA-044 through this correction. |
| **QA-047** | Composite EXECUTION_RESULT membership resolved only the execution/verdict rather than the immutable ExecutedValidationResult and its full bidirectional evidence provenance. | **Independently verified closed** | Regression watch. | Repository retrieval, result-payload hash verification, attempt/execution/run/evidence bidirectional checks and explicit `executed_result_id` links run at assembly and finalisation. Independent re-review accepted the complete negative coverage. |
| **QA-048** | Post-entry SUSPENSION_RESULT membership and evidence export did not resolve/preserve the complete attempt→execution→run→evidence provenance chain, and no standalone finalised-suspension export path existed. | **Independently verified closed — accepted DC-004/DC-005 application baseline** | Regression watch. | Final review accepted the shared provenance resolver, standalone/composite source set and exact active/historical catalogue/test/case resolution gate. Historical v1.0→active v1.1 export preserves original definition identity and separate generation build; missing/tampered/mismatched sources reject without re-evaluating VSC truth. |
| **QA-049** | `evaluation_type` directly selected the VSC condition and the record stored generic gate labels rather than actual deterministic classifier outcomes. | **Independently verified closed — accepted DC-005 application baseline** | Regression watch. | Final review accepted backend integrity/identity/time precedence plus unique exact target/test/case/field-or-step reviewer-gate scope. Applicable higher gates cannot be bypassed by lower routing hints and duplicate same-gate records reject deterministically. |
| **QA-050** | DC-006 source registration accepted arbitrary owner/type and caller-shaped `selector_values`, so tests could copy the criterion answer key rather than extract observations from controlling records. | **Independently verified closed** | Regression watch under final DC-006 application review. | Final independent review at `4ba232a1…5cb47` accepts the complete eight-family backend producer/resolver boundary. Producers read actual controlling authorities, accept identity/context rather than observation payloads, create immutable source snapshots and enforce exact origin/provenance/role membership; the public workflow cannot inject arbitrary payloads or select a favourable source. The shared controlled actor registry remains the single local reviewer-role authority. |
| **QA-051** | `CriterionDefinition.normalisation` was stored but ignored by the primitive comparison engine. | **Independently verified closed** | Regression watch under final DC-006 application review. | Independent re-review at `c7879bd5…9b661` accepts the exact catalogue-v1.2 normalisation-profile registry execution before every primitive operator, including unsupported-profile rejection, canonical/ordered/set treatment, numeric units/precision, controlled scalar/boolean profiles and DET-03 exclusion. Catalogue/method/criterion definitions and hashes remain unchanged. |
| **QA-052** | Fixture, preserved-record and engineering-review procedures could complete without owning a real `ValidationExecution`. | **Independently verified closed** | Regression watch under final DC-006 application review. | Independent re-review at `c7879bd5…9b661` accepts one immutable `ValidationExecution` for every performed non-composite procedure, with attempt → execution → context → findings → result traceability, exact scenario versus non-scenario lifecycle separation and pre-entry DC-005 suspension retained as the only no-execution route. |
| **QA-053** | Catalogue-wide producer-to-role-to-selector closure and actual formal/scenario/fixture conformance were not yet demonstrated for all 35 methods and 214 criteria. | **Corrected — pending final independent re-review** | Final independent DC-006 application re-review. | Required lifecycle roots now come only from actual backend command/result, replay, invalidation, execution, reset and history records. Missing facts resolve to NOT_EVALUATED/INCOMPLETE/no result. All eight compound-selector families have fact-derived semantics and mismatch negatives; exact nine/four DC-004 methods pass through real scenarios, including A4 fresh/stale, A2/B2 PERMITTED, A1 REJECTED and A4 NO_CANDIDATE. No expected-outcome lookup was added. |
| **QA-054** | Deterministic-repeat and evidence-package methods did not yet prove their exact accepted role memberships and source identities without loose historical aggregation. | **Corrected — pending final independent re-review** | Final independent DC-006 application re-review. | Repeat production resolves only after exact formal, negative-fixture and corrected-v1.1 pairs satisfy completion, configuration, active catalogue/test/method/build, fixture/clock fingerprint, distinct identity and single-link gates. Invalid membership stays INCOMPLETE/no result. DET-05 compares repository-resolved execution/evidence/result and applicable correction hashes, not hash-string shape. Exact package roles and historical resolution remain preserved. |
| **QA-055** | NFR determination could rely on declared surface/record evidence rather than proving the exact implemented eight-surface notice/profile set and frozen 45-record ownership registry. | **Corrected — pending final independent re-review** | Final independent DC-006 application re-review. | The exact eight-surface registry and 45-record ownership registry remain controlled. NFR-M06 now resolves a separate concrete child-component field-binding contract; breaking an actual field token fails while registry/profile prose remains unchanged. Existing notice/profile/surface and structural negatives remain, and subjective qualities remain reviewer-only. |

## Current gates

1. **Completed:** DC-003 independently re-reviewed, accepted, applied to all six affected authoritative artefacts and cross-document verified.
2. **Completed:** Validation Plan v1.0 finalised as the accepted Step 9 baseline with 24 catalogue tests and 124/124 RTM coverage.
3. **Accepted I1 implementation baseline:** independent engineering/implementation review accepted commit `ed8ba219caaf23ff5d32dd07893ddcc5ab0163e7`, including IMP-001 resolution, immutable v1.0/v1.1 implementation packages, deterministic LF policy, manifest identity consistency, schema/hash/tamper/single-difference verification, build-identity assembly coverage, initial migration foundation and I1 tests.
4. **Accepted I2 implementation baseline:** independent engineering/implementation review accepted commit `e2aa7c0742a4df0867fc7624a59a4d104dd8dba0`, including the QA-003 implementation treatment and independently verified closure of QA-021, QA-022 and QA-023. The reviewed I2 history and administrative acceptance record are incorporated into `main`.
5. **Accepted I3 implementation baseline:** independent engineering/implementation review accepted branch tip `b22e5a50cdfc2a9ef34bb8b7657887c520e0b201`, including independently verified closure of QA-024, QA-025, QA-026, QA-027 and QA-028. The reviewed I3 history and administrative acceptance record are incorporated into `main`.
6. **Accepted I4 implementation baseline:** independent engineering/implementation review accepted branch tip `702d198fffed6bf9b330f3c349090a47b5b2d4db`, including independently verified closure of QA-029 and QA-030. The reviewed I4 history and administrative acceptance record are incorporated into `main`.
7. **Accepted I5 implementation baseline:** independent engineering/implementation review accepted branch tip `ae4d39102755c733def73746331c4e602845e2d0`, including independently verified closure of QA-031 and QA-032. The database closes the final evidence set; the unchanged catalogue exactly matches all 286 accepted Section 15 relationships across 124 requirements. The reviewed I5 history and administrative acceptance record are incorporated into `main`.
8. **Accepted I6 implementation baseline:** independent engineering/implementation review accepted branch tip `36c2aa8568eacddc5505677609c812706ec9c345`, including the accepted QA-003 presentation treatment and independently verified closure of QA-033, QA-034 and QA-035. The reviewed I6 history and administrative acceptance record are incorporated into `main`.
9. **Accepted I7 implementation baseline:** final independent engineering/implementation review accepted branch tip `d6c2edd1cd90941d8263fcdab94d8b363372fbcc`, including accepted QA-036 treatment and independently verified closure of QA-037, QA-038 and QA-039. The accepted immutable failure/investigation/correction/repeat/regression chain and all I1–I6 authorities remain unchanged.
10. **Accepted I8 implementation baseline:** final independent engineering/implementation review accepted reviewed branch tip `111df44a425731dc7f44c437c1d675e5dac85263`, including QA-008 as implemented and independently verified closure of QA-040. The accepted Exploration, evidence-export and all I1–I7 authorities remain unchanged.
11. **Accepted DC-004 design baseline:** final independent engineering review accepted reviewed design tip `69f615a0ddfa2d78847c48a6c305682e2f2a94a2`. Its Validation Plan Section 19 and Demonstrator Design Section 36 treatment is retained in current v1.3/v0.5; DQ-002 is resolved at design level without changing the accepted I8 machine/runtime baseline.
12. **Accepted DC-004 application baseline:** final independent application review accepted exact PR #10 tip `eced7c06c27b959cdb29d3aaa9351ca11cb5e258`; the complete reviewed history is incorporated into `main` without rewriting commits.
13. **Accepted DC-005 design and application baseline:** Requirements Specification v0.4 clarification at REQ-VAL-007–009, Validation Plan v1.3 Section 20, Demonstrator Design v0.5 Section 37, System Architecture v0.4 Section 27 and Workflow Design v0.4 Section 28 remain authoritative. The independently accepted machine/application treatment closes QA-042 and QA-041 through QA-049 for this boundary.
14. **Accepted DC-006 design and authoritative-document baseline:** final independent design review accepted DR-01–DR-07, and authoritative-document re-review accepted the corrected technical application at exact tip `c19451134c36d13d54f2185a3eaa0f20fcce95f0`; AA-01–AA-04 are closed. Validation Plan v1.3 Section 21, System Architecture v0.4 Section 28, Workflow Design v0.4 Section 29 and Demonstrator Design v0.5 Section 38 are authoritative. The accepted application contains 214 criteria (147 direct-test plus 67 DC-004 constituent-case criteria) as a reviewed document identity, not a permanent invariant.
15. **DC-006 machine/catalogue application review gate:** QA-050 is independently closed at reviewed tip `4ba232a1…5cb47`; QA-051 and QA-052 remain independently closed at `c7879bd5…9b661`. QA-053–QA-055 are corrected on the same application branch, preserving active catalogue v1.2 at 35 methods / 214 criteria / 24 tests / 124 requirements / 286 RTM pairs / 15 events. The complete application boundary remains pending final independent re-review and is not yet an accepted implementation baseline.
16. **Current gate:** I8 plus accepted DC-004/DC-005 machine/application and accepted DC-006 authoritative documents remain the accepted baseline. The QA-053–QA-055-corrected DC-006 machine/catalogue application awaits final independent re-review and merge authorisation. I9 remains stopped.

## V2 Automation Candidates

**V2 Automation Candidate — register reconciliation and impact analysis.** Rechecking statuses across change records, detailed documents, requirements, tests and manifests is repetitive and evidence-heavy; a future assurance assistant could identify candidate status changes and affected artefacts while leaving approval to the engineer.

**V2 Automation Candidate — traceability completeness.** Mechanical detection of missing, duplicated or stale requirement-to-test links would reduce manual RTM effort and regression risk while preserving engineering review of whether each mapping is substantively correct.

**V2 Automation Candidate — consequence-to-source investigation assembly.** Collecting telemetry, source-path, topology, OMS, configuration-difference and provenance evidence into a reviewable root-cause chain is repetitive and evidence-heavy. A future assurance assistant could assemble and cross-check that evidence while leaving defect judgement and correction approval to the engineer.
